AC_INIT(Makefile.am)

dnl If you change the version here, also change it in version.h
AM_INIT_AUTOMAKE(bnf, 1.6.10)
AM_CONFIG_HEADER(config.h)

AC_PROG_CC
AC_STDC_HEADERS

AC_OUTPUT(Makefile src/Makefile doc/Makefile etc/Makefile)
